<Page x:Class="Vue.MainMenuPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Background="Azure"
	Title="MainMenuPage">

    <Page.Resources>
        <Style
            x:Key="MainButtonStyle"
            BasedOn="{StaticResource {x:Type Button}}"
            TargetType="Button">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style
            x:Key="AccountButtonsStyle"
            BasedOn="{StaticResource MainButtonStyle}"
            TargetType="Button">
            <Setter Property="Height" Value="20"/>
            <Setter Property="Width" Value="88"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
        </Style>
    </Page.Resources>


    <Grid>
        <TextBlock Height="24" Margin="221,31,221,0" TextWrapping="Wrap" Width="75" VerticalAlignment="Top" FontWeight="Bold"><Run Text="Arch'Image"/><LineBreak/><Run/></TextBlock>
        <Button x:Name="MapButton" Margin="10,92,0,298" Click="Todo" Style="{StaticResource MainButtonStyle}">
            <StackPanel>
                <Image Source="Resources/trans.png" HorizontalAlignment="Center"/>
            </StackPanel>
        </Button>
        <Button x:Name="RMButton" Content="Registres matricules" Margin="0,92,0,298" Click="RMButton_Click" Style="{StaticResource MainButtonStyle}" HorizontalAlignment="Center"/>
        <Button x:Name="FavorisButton" Content="Favoris" Margin="507,92,10,298" Click="Todo" Style="{StaticResource MainButtonStyle}" HorizontalAlignment="Right"/>
        <Button x:Name="NMDButton" Content="NMD" Margin="10,232,0,158" Click="NMDButton_Click" Style="{StaticResource MainButtonStyle}"/>
        <Button x:Name="RecensementButton" Content="Recensement" Margin="0,232,0,158" Click="RecensementButton_Click" Style="{StaticResource MainButtonStyle}" HorizontalAlignment="Center"/>
        <Button x:Name="ParametresButton" Content="Paramètres" Margin="507,232,10,158" Click="Todo" Style="{StaticResource MainButtonStyle}" HorizontalAlignment="Right"/>

        <Button x:Name="ConnexionButton" Content="Connexion" Margin="10,0,0,10" Click="ConnexionButton_Click" Style="{StaticResource AccountButtonsStyle}"/>
        <Button x:Name="InscriptionButton" Content="Inscription" Margin="103,0,0,10" Click="InscriptionButton_Click" Style="{StaticResource AccountButtonsStyle}"/>
    </Grid>

</Page>
