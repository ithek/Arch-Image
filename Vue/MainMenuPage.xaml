<Page x:Class="Vue.MainMenuPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Background="Azure"
	Title="MainMenuPage">

    <Page.Resources>
        <Style
            x:Key="MainButtonStyle"
            BasedOn="{StaticResource {x:Type Button}}"
            TargetType="Button">
            <Setter Property="Width" Value="180"/>
            <Setter Property="Height" Value="75"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style
            x:Key="AccountButtonsStyle"
            BasedOn="{StaticResource MainButtonStyle}"
            TargetType="Button">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
        </Style>
    </Page.Resources>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Image Grid.Row="0" Grid.Column="2" Source="Resources/logo_archimage.png" VerticalAlignment="Top" Margin="0,50,0,0" />
        <Button Grid.Row="1" Grid.Column="1" x:Name="MapButton" Click="MapButton_Click" Style="{StaticResource MainButtonStyle}">
            <StackPanel>
                <Image Source="Resources/trans.png" HorizontalAlignment="Center"/>
            </StackPanel>
        </Button>
        <Button Grid.Row="1" Grid.Column="2" x:Name="RMButton" Content="Registres matricules" Click="RMButton_Click" Style="{StaticResource MainButtonStyle}" HorizontalAlignment="Center"/>
        <Button Grid.Row="1" Grid.Column="3" x:Name="RestoreSessionButton" Content="Restaurer la session" Click="RestoreSessionButton_Click" Style="{StaticResource MainButtonStyle}" HorizontalAlignment="Right"/>
        <Button Grid.Row="2" Grid.Column="1" x:Name="NMDButton" Content="NMD" Click="NMDButton_Click" Style="{StaticResource MainButtonStyle}"/>
        <Button Grid.Row="2" Grid.Column="2" x:Name="RecensementButton" Content="Recensement" Click="RecensementButton_Click" Style="{StaticResource MainButtonStyle}" HorizontalAlignment="Center"/>
        <Button Grid.Row="2" Grid.Column="3" x:Name="ParametresButton" Content="Paramètres" Click="Todo" Style="{StaticResource MainButtonStyle}" HorizontalAlignment="Right"/>

        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal">
            <Button x:Name="ConnexionButton" Margin="0,0,10,0" Content="Connexion" Click="ConnexionButton_Click" Style="{StaticResource AccountButtonsStyle}"/>
            <Button x:Name="InscriptionButton" Content="Inscription" Click="InscriptionButton_Click" Style="{StaticResource AccountButtonsStyle}"/>
        </StackPanel>

        <Button Grid.Row="3" Grid.Column="3" x:Name="FavorisButton" Content="Favoris" Click="Todo" Style="{StaticResource MainButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>


    </Grid>

</Page>
